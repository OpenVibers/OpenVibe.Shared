'use strict';
// Deprecated alias: openvibe-shared/chrome-ssr is now openvibe-shared/frame (the OpenVibe Frame). Kept so
// sites on older code keep working; it goes away in 2.0.0.
module.exports = require('./frame');
